
  # UniScope UI Design

  This is a code bundle for UniScope UI Design. The original project is available at https://www.figma.com/design/hmyo3dtWQMf9QG3Igi6QnS/UniScope-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  